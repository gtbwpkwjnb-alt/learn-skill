# learn-skill dsh profile bundle: registers the SKILL.md at this package root
# on ctx.skills. Applied after @deepseek-ai/dsh-base, which already mounts the
# skill registry and the model-facing skill loader.
- insert:
    - id: learn-skill
      name: 'learn-skill'
